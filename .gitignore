/target
# `cargo deb --output dist/` — release artefacts, built in CI and sometimes
# locally when reproducing a packaging problem.
/dist
.env
node_modules/
package-lock.json
__pycache__/
*.pyc
.env
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.vscode/
.idea/
.DS_Store
.cursorrules
.cursorignore
._next/
.next/
*.db

# Claude Code local session/runtime state (scheduled-task locks etc.) —
# machine-local, not repo content.
.claude/
